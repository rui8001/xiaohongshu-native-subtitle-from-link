# Repository guidance

This repository is one independently installable Codex Skill. Keep `SKILL.md` at the repository root and keep the folder, frontmatter name, examples, UI metadata, scripts, and README consistent with `xiaohongshu-native-subtitle-from-link`.

Never commit real downloaded media, output images, personal paths, credentials, cookies, account data, private sources, or copyrighted examples. Use synthetic, self-created, or clearly redistributable test material.

Validate `SKILL.md`, parse YAML and JSON, compile Python, check the zsh entry, run the synthetic renderer and packager test, inspect output dimensions, and scan for secrets before a release. Preserve automatic invocation unless a maintainer explicitly changes the policy.
